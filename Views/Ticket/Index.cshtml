@model List<MovieTheater.Models.Invoice>
@{
    ViewData["Title"] = "My Tickets";
}

<div class="container mt-4">
    <h2 class="text-center mb-4">My Tickets</h2>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- AC-03: Navigation Links -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="btn-group" role="group">
                <a asp-controller="Account" asp-action="Profile" class="btn btn-outline-primary">
                    <i class="fas fa-user"></i> Account Information
                </a>
                <a asp-controller="Ticket" asp-action="Index" class="btn btn-outline-primary">
                    <i class="fas fa-history"></i> All Tickets
                </a>
                <a asp-controller="Ticket" asp-action="Booked" class="btn btn-outline-primary">
                    <i class="fas fa-ticket-alt"></i> Booked Tickets
                </a>
                <a asp-controller="Ticket" asp-action="Canceled" class="btn btn-outline-primary">
                    <i class="fas fa-ban"></i> Canceled Tickets
                </a>
            </div>
        </div>
    </div>

    <!-- AC-01: Ticket List -->
    @if (Model != null && Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-bordered table-striped">
                <thead class="table-dark">
                    <tr>
                        <th>Booking Date</th>
                        <th>Movie</th>
                        <th>Show Date</th>
                        <th>Show Time</th>
                        <th>Seats</th>
                        <th>Total Amount</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var booking in Model)
                    {
                        <tr>
                            <td>@booking.BookingDate?.ToString("dd/MM/yyyy HH:mm")</td>
                            <td>@booking.MovieName</td>
                            <td>@booking.ScheduleShow?.ToString("dd/MM/yyyy")</td>
                            <td>@booking.ScheduleShowTime</td>
                            <td>@booking.Seat</td>
                            <td class="text-end">@booking.TotalMoney?.ToString("N0") VND</td>
                            <td>
                                @if (booking.Status == 1)
                                {
                                    <span class="badge bg-success">Booked</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">Canceled</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group">
                                    <a asp-controller="Ticket" asp-action="Details" asp-route-id="@booking.InvoiceId" 
                                       class="btn btn-sm btn-info">
                                        <i class="fas fa-info-circle"></i> Details
                                    </a>
                                    @if (booking.Status == 1 && booking.ScheduleShow.HasValue && 
                                         booking.ScheduleShow.Value.AddHours(-24) > DateTime.Now)
                                    {
                                        <form asp-controller="Ticket" asp-action="Cancel" asp-route-id="@booking.InvoiceId" 
                                              method="post" class="d-inline" 
                                              onsubmit="return confirm('Are you sure you want to cancel this ticket?');">
                                            <button type="submit" class="btn btn-sm btn-danger">
                                                <i class="fas fa-times"></i> Cancel
                                            </button>
                                        </form>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <!-- AC-02: No tickets message -->
        <div class="alert alert-info text-center">
            <i class="fas fa-info-circle me-2"></i>
            No booked tickets found.
        </div>
    }
</div>

@section Styles {
    <style>
        .table th {
            white-space: nowrap;
        }
        .badge {
            font-size: 0.9em;
            padding: 0.5em 0.8em;
        }
        .btn-group {
            gap: 0.5rem;
        }
    </style>
} 