@{
    ViewData["Title"] = "My Account";
}

<div class="container mt-5">
    <h2 class="text-center mb-4">My Account</h2>

    <ul class="nav nav-tabs" id="accountTabs" role="tablist">
        <li class="nav-item">
            <a class="nav-link active" href="@Url.Action("Profile", "Account")">Account Info</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@Url.Action("ScoreHistory", "Account")">Score History</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@Url.Action("Booked", "Ticket")">Booked Tickets</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@Url.Action("Canceled", "Ticket")">Canceled Tickets</a>
        </li>
    </ul>

    <div class="tab-content mt-4">
        <div class="tab-pane fade show active">
            <p>Welcome to your account dashboard!</p>
            <p>Use the tabs above to navigate your information.</p>
        </div>
    </div>
</div>
